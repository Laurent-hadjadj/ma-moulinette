{% set title = 'Suivi des indicateurs du projet' %}
{% set description = 'Ma Moulinette - Suivi des versions.' %}
{% set type_content = "Application Ma-Moulinette, Suivi des indicateurs SonarQube pour le projet." %}
{% set bottom="footer-fixed" %}
<!DOCTYPE html>
<!--
.. __  __             __  __             _ _            _   _
  |  \/  | __ _      |  \/  | ___  _   _| (_)_ __   ___| |_| |_ ___
  | |\/| |/ _` |_____| |\/| |/ _ \| | | | | | '_ \ / _ \ __| __/ _ \
  | |  | | (_| |_____| |  | | (_) | |_| | | | | | |  __/ |_| ||  __/
  |_|  |_|\__,_|     |_|  |_|\___/ \__,_|_|_|_| |_|\___|\__|\__\___|

  Laurent HADJADJ
  https://github.com/Laurent-hadjadj/ma-moulinette
  © 2024 - CC BY-SA-NC 4.0
-->
<html	lang="fr">
  {# Chargement de la section HEAD #}
  <head>
    <title>{{ title }}</title>
    {{ include('head.html.twig') }}
    {# Chargement des assets JS et CSS #}
    {% block importmap %}{{ importmap('suivi') }}{% endblock %}
  </head>

  <body dir="ltr">
    {# no-script #}
    {{ include('no-script.html.twig') }}
    {# Chargement de la section header #}
    {{ include('header.html.twig') }}

    <header class="grid-container margin-top-1">
      <nav aria-label="Fil d'Ariane, vous êtes ici">
        <ol class="claire-hand breadcrumbs">
          <li><a href="{{ path('accueil', {}) }}">Accueil</a></li>
          <li><a href="{{ path('projet', {}) }}">Projet</a></li>
          <li aria-current="page">Suivi</li>
        </ol>
      </nav>
    </header>

    {# Message d'information php #}
    {{ include('_message.html.twig') }}

    <header id="main-content"
            class="grid-container"
            aria-labelledby="titre-sonarqube">
      <h2 id="titre-sonarqube"
          class="h3 claire-hand"><span aria-hidden="true">📊</span>Collecte et analyse des indicateurs SonarQube
      </h2>
    </header>

    {# En-tête #}
    <header class="pleine-page">
      <p id="titre-js-nom"
          class="open-sans"
          data-maven="{{mavenKey}}"><span aria-hidden="true">🧭</span>Suivi des indicateurs pour <strong>{{nom}}</strong>
      </p>
      <div class="show-for-sr">
        Cette page présente les principaux indicateurs de suivi du projet et permet de générer un rapport PDF, d'ajouter une version dans l'historique et de modifier la version de référence ou favorite.
      </div>

      {% set nombre=suivi|length %}
      {% if nombre>1 %}
        {% set s="s"%}
      {% else %}
        {% set s="" %}
      {% endif %}
      <p class="open-sans font-size-11">
          <span id="affiche-total-profil" class="mini-stat">
            <span aria-hidden="true">🗂️</span>{{nombre | default('0') }}</span>
            <span>version{{s}} sélectionnée{{s}} pour ce projet.</span>
          </span>
      </p>
    </header>

    {# Menu des outils  #}
    {{ include('/suivi/_menu_outils.html.twig') }}

    {# Page de garde rapport PDF. #}
    {{ include('/suivi/_suivi_synthese_notes.html.twig') }}

    {# Affichage du suivi des anomalies #}
    {{ include('/suivi/_suivi_anomalie.html.twig') }}

    {# Affiche le graphique des courbes des anomalies #}
    {{ include('/suivi/_affiche_graphique_anomalie.html.twig') }}
    {{ include('/suivi/_suivi_mesures.html.twig') }}
    {# Tests & Couverture (cf. project_suivi_redesign.md). #}
    {{ include('/suivi/_suivi_tests_couverture.html.twig') }}
    {# Complexité (cyclomatique + cognitive). #}
    {{ include('/suivi/_suivi_complexite.html.twig') }}
    {# Distribution par langage (tableau + bar chart empile). #}
    {{ include('/suivi/_suivi_distribution_langage.html.twig') }}
    {{ include('/suivi/_repartition_par_module.html.twig') }}
    {{ include('/suivi/_repartition_par_severity.html.twig') }}

    {# Affiche le tableau des anomalies par sévérité détaillée #}
    {{ include('/suivi/_repartition_anomalie_par_severity.html.twig') }}

    {#-- fenêtre modale --#}
    {{ include('/suivi/_modal_ajouter_une_analyse.html.twig') }}
    {{ include('/suivi/_modal_modifier_les_versions.html.twig') }}

    {# Chargement de la section footer #}
    {{ include('footer.html.twig') }}
  </body>
</html>
