{% set title = 'Suivi des versions' %}
{% set description = 'Ma Moulinette - Suivi des versions.' %}
{% set bottom="footer-fixed" %}
<!DOCTYPE html>
<!--
.. __  __             __  __             _ _            _   _
  |  \/  | __ _      |  \/  | ___  _   _| (_)_ __   ___| |_| |_ ___
  | |\/| |/ _` |_____| |\/| |/ _ \| | | | | | '_ \ / _ \ __| __/ _ \
  | |  | | (_| |_____| |  | | (_) | |_| | | | | | |  __/ |_| ||  __/
  |_|  |_|\__,_|     |_|  |_|\___/ \__,_|_|_|_| |_|\___|\__|\__\___|

  Laurent HADJADJ
  https://github.com/Laurent-hadjadj/ma-moulinette
  © 2024 - CC BY-SA-NC 4.0
-->
<html	lang="fr">
  {# Chargement de la section HEAD #}
  <head>
    <title>{{ title }}</title>
    {{ include('head.html.twig') }}
    {% block importmap %}{{ importmap('suivi') }}{% endblock %}
  </head>

  <body>
    {# Chargement de la section header #}
    {{ include('header.html.twig') }}
    <header class="grid-container">
      <nav aria-label="vous êtes Ici : ">
        <ul class="claire-hand breadcrumbs">
          <li><a href="{{ path('accueil', {}) }}">Accueil</a></li>
          <li><a href="{{ path('projet', {}) }}">Projet</a></li>
          <li><span class="show-for-sr">Vous êtes Ici :</span>Suivi</li>
        </ul>
      </nav>
    </header>

    <div class="grid-container grid-x">
    {% for message in app.flashes('notice') %}
      <div class="callout {{message.type}} alert-callout-border callout-ombre" data-closable>
        <p class="open-sans text-justify callout-message"><strong>{{message.reference}}</strong> {{ message.message }}</p>
        {% if app.environment == 'dev' and message.debug is defined %}
          <p class"open-sans">{{message.debug}}</p>
        {% endif %}
        <button class="close-button {{message.type}}" aria-label="Dismiss alert" type="button" data-close>
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    {% endfor %}
    </div>

    {# En-tête #}
    <div id="element1-to-print" class="pleine-page">
      <h2 id="js-nom" class="claire-hand" data-maven="{{mavenKey}}">{{nom}}</h2>
      <div class="cell">
        <h1 class="h4 claire-hand">
          <span id="affiche-total-profil" class="stat">
            {% set nombre=suivi|length %}
            {% if nombre>1 %}
              {% set s="s"%}
            {% else %}
              {% set s="" %}
            {% endif %}
            {{nombre | default('0') }}</span>
          version{{s}} sélectionné{{s}}.
        </h1>
      </div>
    </div>

    {# Affichage du suivi des anomalies #}
    {{ include('/suivi/_suivi_anomalie.html.twig') }}

    {# Menu des outils  #}
    {{ include('/suivi/_menu_outils.html.twig') }}

    {# Affiche le graphique des courbes des anomalies #}
    {{ include('/suivi/_affiche_graphique_anomalie.html.twig') }}
    {{ include('/suivi/_suivi_mesures.html.twig') }}
    {{ include('/suivi/_repartition_par_module.html.twig') }}
    {{ include('/suivi/_repartition_par_severity.html.twig') }}

    {# Affiche le tableau des anomalies par sévérité détaillée #}
    {{ include('/suivi/_repartition_anomalie_par_severity.html.twig') }}

    {#-- fenêtre modale --#}
    {{ include('/suivi/_modale_ajouter_une_analyse.html.twig') }}
    {{ include('/suivi/_modale_modifier_les_versions.html.twig') }}

    <br><br>
    {# Chargement de la section footer #}
    {{ include('footer.html.twig') }}
  </body>
</html>
