/**
 * Copiez ce fichier en secrets.local.js (ignoré par git) et renseignez vos
 * valeurs locales si nécessaire. Un token vide/null est un fonctionnement
 * nominal : le code appelant gère son absence.
 */
export const token = null;
