---
# MODIF 2026-05-24 : suppression de activity_queue et du routing
# ProcessTaskMessage — la collecte Activity est migrée vers ActivityCollecteCommand.
framework:
    messenger:
        failure_transport: failed
        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'
        routing: {}

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
...
